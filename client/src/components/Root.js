import React from 'react';
import FormMain from './Form/FormMain';

const Root = (props) => {
    return (
        <div>
            <h1>Main component</h1>
        </div>
    );
};

export default Root;